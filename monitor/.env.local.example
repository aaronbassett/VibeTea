# VibeTea Monitor Persistence Configuration
# Copy this file to .env.local and fill in values

# Supabase Edge Functions URL
# Enable persistence by setting this to your Supabase functions endpoint
# Comment out or leave empty to disable persistence (events only sent via WebSocket)
VIBETEA_SUPABASE_URL=http://127.0.0.1:54321/functions/v1

# Batch interval in seconds (default: 60)
# Events are batched and sent at this interval OR when 1000 events are queued
VIBETEA_SUPABASE_BATCH_INTERVAL_SECS=60
