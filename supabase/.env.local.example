# Supabase Edge Functions Environment Configuration
# Copy this file to .env.local and fill in values from `supabase start` output

# Supabase URLs (from supabase start output)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# VibeTea-specific configuration
# Token for query endpoint authentication (client uses this)
VIBETEA_SUBSCRIBER_TOKEN=dev-token-for-testing

# Public keys for Ed25519 signature verification (base64-encoded)
# Format: source_id:public_key (comma-separated for multiple)
# Generate with: base64 < ~/.config/vibetea/server.pub
VIBETEA_PUBLIC_KEYS=dev-monitor:MCowBQYDK2VwAyEA...
