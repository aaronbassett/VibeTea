# VibeTea Client Environment Variables

# WebSocket URL for the VibeTea server
# If not set, defaults to ws(s)://{current_host}/ws
VITE_WS_URL=wss://vibetea-server.fly.dev/ws

# Supabase Edge Functions URL (optional)
# Enable persistence features (historic data heatmap) by setting this
# Comment out or leave empty to disable persistence features
# VITE_SUPABASE_URL=http://127.0.0.1:54321/functions/v1

# Supabase subscriber token for query endpoint authentication
# VITE_SUPABASE_SUBSCRIBER_TOKEN=dev-token-for-testing
